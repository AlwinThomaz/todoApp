let myName = 'Alwin';

function sayName() {
    let secret = 'watchandcode';
    console.log(myName);
}

sayName();
console.log(secret);

//if you are inside of a function, you can look out and see data, but the opposite is not true. 
//If you are outside, you cannot look in.

