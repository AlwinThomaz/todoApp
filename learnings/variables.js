"use script";
// 'use script' , once used cannot go back, not really neeeded with modern JS where classes and modules are introduced


// 'let' is the standard
// 'var' is the older variant
// must only contain letters, digits, $ and _
// cannot start with a digit
//naming should follow camelCase

// constant variable -  cannot be reassigned, only used for hard-coded values
// const aNumber = 5;
// for values that are known before execution such as const COLOUR_ORANGE = "FF&F00"

